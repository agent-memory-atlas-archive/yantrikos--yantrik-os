// Re-export of the UI kit's message_bubble.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/message_bubble.slint -- edit that file, not this one.)
export { MessageData, ContentBlock, MessageBubble } from "../../../yantrik-ui-kit/slint/message_bubble.slint";
