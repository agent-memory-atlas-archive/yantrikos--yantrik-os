// Re-export of the UI kit's agent_rail.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/agent_rail.slint — edit that file, not this one.)
export { AgentRail, AgentContextItem, AgentSuggestion } from "../../../yantrik-ui-kit/slint/agent_rail.slint";
