// Re-export of the UI kit's app_header.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/app_header.slint — edit that file, not this one.)
export { AppAction, AppHeader } from "../../../yantrik-ui-kit/slint/app_header.slint";
