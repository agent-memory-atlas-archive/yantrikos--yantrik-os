// Re-export of the UI kit's line_chart.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/line_chart.slint -- edit that file, not this one.)
export { LineChart } from "../../../yantrik-ui-kit/slint/line_chart.slint";
