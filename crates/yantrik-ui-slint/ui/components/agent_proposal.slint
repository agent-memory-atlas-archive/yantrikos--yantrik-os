// Re-export of the UI kit's agent_proposal.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/agent_proposal.slint — edit that file, not this one.)
export { AgentProposalCard, AgentProposal } from "../../../yantrik-ui-kit/slint/agent_proposal.slint";
