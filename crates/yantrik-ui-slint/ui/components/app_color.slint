// Re-export of the UI kit's app_color.slint so the shell and every app share one palette.
// (crates/yantrik-ui-kit/slint/app_color.slint -- edit that file, not this one.)
export { AppColor, AppIdentity } from "../../../yantrik-ui-kit/slint/app_color.slint";
