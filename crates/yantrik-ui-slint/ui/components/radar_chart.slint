// Re-export of the UI kit's radar_chart.slint so the shell and every app share one implementation.
// (crates/yantrik-ui-kit/slint/radar_chart.slint -- edit that file, not this one.)
export { RadarChart } from "../../../yantrik-ui-kit/slint/radar_chart.slint";
