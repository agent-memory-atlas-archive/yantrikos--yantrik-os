// Re-export of the UI kit's tool_call.slint: one card for a mind's tool call, wherever it is shown.
// (crates/yantrik-ui-kit/slint/tool_call.slint -- edit that file, not this one.)
export { ToolCallData, ToolCallCard } from "../../../yantrik-ui-kit/slint/tool_call.slint";
