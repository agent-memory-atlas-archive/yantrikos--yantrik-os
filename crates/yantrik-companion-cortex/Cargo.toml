[package]
name = "yantrik-companion-cortex"
version = "0.1.0"
edition = "2021"
description = "Context Cortex — cross-system intelligence framework for the Yantrik companion"

[dependencies]
yantrik-companion-core = { workspace = true }
# meeting_prep reads the machine's calendar rather than a table of its own. Neither of these
# pulls in Slint.
yantrik-ipc-contracts = { workspace = true }
yantrik-ipc-transport = { workspace = true }
chrono = { workspace = true }
rusqlite = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tracing = { workspace = true }
