//! Nothing here but tests. `tests/desktop_files.rs` reads every `.desktop` file the guide in
//! `docs/sdk` quotes with the shell's own parser; `tests/describe.rs` holds the Rust template to
//! the describe the guide prints. `test_guide.py`, beside this crate, holds every code block in
//! the guide to the file it is quoted from.
